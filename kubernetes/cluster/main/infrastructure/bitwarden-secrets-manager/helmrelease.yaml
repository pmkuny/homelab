apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: longhorn
  namespace: longhorn-system
spec:
  interval: 30m

  targetNamespace: external-secrets
  chart:
    spec:
      chart: sm-operator
      version: "2.0.3"
      sourceRef:
        kind: HelmRepository
        name: bitwarden
