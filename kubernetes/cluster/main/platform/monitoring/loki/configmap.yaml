apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-loki-cm
  namespace: logs
data:
  values.yaml: |
    loki:
      storage:
        type: s3
        s3:
          endpoint: ${LOKI_S3_ENDPOINT}
          region: ${LOKI_S3_REGION}
