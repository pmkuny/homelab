apiVersion: v1
kind: ConfigMap
metadata:
  name: alloy-cm
  namespace: monitoring
data:
  alloy.yaml: |
    # UI Serving
    enableHttpServerPort: true
    listenAddr: 0.0.0.0
    listenPort: 80
    listenSchem: HTTP
    # Readiness and Timeout
    initialDelaySeconds: 10
    timeoutSeconds: 1
