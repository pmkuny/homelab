apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: infra-bitwarden-secrets-manager
  namespace: external-secrets
spec:
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  path: ./kubernetes/cluster/main/infrastructure/bitwarden-secrets-manager/
  prune: true
